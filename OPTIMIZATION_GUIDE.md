# 选股程序性能优化指南

## 当前性能瓶颈

### 1. 数据加载效率低
- 逐个读取CSV文件，没有并行化
- 每次选股都要重新加载所有数据

### 2. 重复计算
- 多个Selector都调用相同的指标计算函数
- 没有缓存机制，相同数据被重复计算

### 3. SuperB1Selector效率特别低
- 在循环中多次调用完整的过滤逻辑
- 时间复杂度为 O(n²)

### 4. 单线程处理
- 所有股票串行处理，没有利用多核CPU

## 优化方案

### 方案1: 预计算常用指标（推荐优先实施）

在加载数据时一次性计算所有常用指标，避免重复计算。

### 方案2: 添加指标缓存机制

使用字典缓存已计算的指标，避免重复计算。

### 方案3: 优化SuperB1Selector

只检查最近的几个关键点，而不是循环所有历史。

### 方案4: 使用多进程并行处理

利用多核CPU并行处理多只股票。

## 预期效果

综合实施以上优化后，预计总体运行时间减少 60-80%